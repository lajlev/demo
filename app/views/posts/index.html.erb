<h1>All posts</h1>
<ul>
  <% @posts.each do |p| %>
    <li>
      <h3><%= link_to p.title, post_path(p.id) %></h3> 
      <p>
        <small>By: <%= p.user.email %></small>
      </p>
      <p><%= p.body %></p>
    </li>
    <% end %>
</ul>